#include <stdio.h>

int countDigits(int n)
{
    if(n == 0)
        return 0;

    return 1 + countDigits(n / 10);
}

int main()
{
    int n;

    printf("Enter Number: ");
    scanf("%d", &n);

    printf("Digits = %d", countDigits(n));

    return 0;
}
Sample Output
Enter Number: 12345
Digits = 5
