#include <stdio.h>

int sumSquares(int n)
{
    if(n == 1)
        return 1;

    return n*n + sumSquares(n - 1);
}

int main()
{
    int n;

    printf("Enter N: ");
    scanf("%d",&n);

    printf("Sum = %d", sumSquares(n));

    return 0;
}
Sample Output
Enter N: 5
Sum = 55
