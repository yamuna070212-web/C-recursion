#include <stdio.h>

int sumDigits(int n)
{
    if(n == 0)
        return 0;

    return (n % 10) + sumDigits(n / 10);
}

int main()
{
    int n;

    printf("Enter a number: ");
    scanf("%d", &n);

    printf("Sum of Digits = %d", sumDigits(n));

    return 0;
}
Sample Output
Enter a number: 1234
Sum of Digits = 10
