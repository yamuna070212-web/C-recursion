#include <stdio.h>

void display(int arr[], int n, int i)
{
    if(i == n)
        return;

    printf("%d ", arr[i]);

    display(arr, n, i + 1);
}

int main()
{
    int arr[] = {10, 20, 30, 40, 50};

    display(arr, 5, 0);

    return 0;
}
Sample Output
10 20 30 40 50
