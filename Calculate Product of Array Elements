#include <stdio.h>

int product(int arr[], int n)
{
    if(n == 1)
        return arr[0];

    return arr[n - 1] * product(arr, n - 1);
}

int main()
{
    int arr[] = {2,3,4,5};

    printf("Product = %d", product(arr,4));

    return 0;
}
Sample Output
Product = 120
